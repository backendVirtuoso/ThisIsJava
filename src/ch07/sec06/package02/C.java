package ch07.sec06.package02;

import ch07.sec06.package01.A;

public class C {
    public void method() {
        // protected는 같은 패키지 내에서만 가능 다른 패키지 불가

        // A a = new A();
        // a.field = "value";
        // a.method();
    }
}
